ShiftPV CSI driver is installed.

StorageClass: {{ .Values.storageClass.name }}
Driver: csi.shiftpv.io
Pool root: {{ .Values.node.poolRoot }}

PVCs, PVs, volume reservation ConfigMaps, and data directories are not Helm-owned.
Before uninstalling, stop workloads that use ShiftPV volumes. Reinstall into the
same namespace with the same pool root to recover retained volumes.
