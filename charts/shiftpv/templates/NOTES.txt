ShiftPV CSI driver chart {{ .Chart.Version }} is installed.

StorageClass: {{ .Values.storageClass.name }}
Driver: csi.shiftpv.io

Use the documentation and operational checks that match this exact chart and
image version. Register one ShiftPVPool per participating node and verify its
version-specific Ready contract before creating PVCs.
{{- if .Values.mobility.enabled }}
Opt in workload namespaces with:
  kubectl label namespace <name> shiftpv.io/admission=enabled
{{- end }}

PVC/PV, installed ShiftPV API objects, capacity holds, and host data are not
disposable Helm state. Do not force-remove finalizers or delete host paths to
bypass the version-specific dependency review.
