apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shiftpv.fullname" . }}-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "shiftpv.labels" . | nindent 4 }}
    app.kubernetes.io/component: controller
spec:
  replicas: {{ .Values.controller.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "shiftpv.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "shiftpv.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: controller
    spec:
      serviceAccountName: {{ include "shiftpv.controllerServiceAccount" . }}
      securityContext:
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: shiftpv-controller
          image: "{{ .Values.controller.image.repository }}:{{ .Values.controller.image.tag }}"
          imagePullPolicy: {{ .Values.controller.image.pullPolicy }}
          command: ["/shiftpv-controller"]
          args:
            - --endpoint=unix:///run/csi/csi.sock
            - --namespace=$(POD_NAMESPACE)
            - --helper-image={{ .Values.helperPod.image }}
            - --helper-timeout={{ .Values.helperPod.timeout }}
            - --helper-cpu-request={{ .Values.helperPod.resources.requests.cpu }}
            - --helper-memory-request={{ .Values.helperPod.resources.requests.memory }}
            - --helper-cpu-limit={{ .Values.helperPod.resources.limits.cpu }}
            - --helper-memory-limit={{ .Values.helperPod.resources.limits.memory }}
            - --mobility-enabled={{ .Values.mobility.enabled }}
            {{- if .Values.mobility.enabled }}
            - --mobility-interval={{ .Values.mobility.interval }}
            - --mobility-helper-image={{ .Values.mobility.helperImage }}
            - --webhook-listen-address=:{{ .Values.mobility.webhookPort }}
            - --webhook-tls-cert-file=/var/run/shiftpv-webhook/tls.crt
            - --webhook-tls-key-file=/var/run/shiftpv-webhook/tls.key
            {{- end }}
            - --v={{ .Values.controller.logLevel }}
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 65532
          volumeMounts:
            - name: socket
              mountPath: /run/csi
            {{- if .Values.mobility.enabled }}
            - name: webhook-tls
              mountPath: /var/run/shiftpv-webhook
              readOnly: true
            {{- end }}
          {{- if .Values.mobility.enabled }}
          ports:
            - name: webhook
              containerPort: {{ .Values.mobility.webhookPort }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: webhook
              scheme: HTTPS
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 2
          {{- end }}
          {{- with .Values.controller.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        - name: csi-provisioner
          image: {{ .Values.sidecars.provisioner.image }}
          args:
            - --csi-address=/run/csi/csi.sock
            - --leader-election=true
            - --leader-election-namespace=$(POD_NAMESPACE)
            - --extra-create-metadata=true
            - --timeout=2m
            - --v={{ .Values.controller.logLevel }}
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 65532
          volumeMounts:
            - name: socket
              mountPath: /run/csi
          {{- with .Values.sidecars.provisioner.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        - name: liveness-probe
          image: {{ .Values.sidecars.livenessProbe.image }}
          args:
            - --csi-address=/run/csi/csi.sock
            - --health-port=9808
            - --v={{ .Values.controller.logLevel }}
          ports:
            - name: healthz
              containerPort: 9808
          livenessProbe:
            httpGet:
              path: /healthz
              port: healthz
            initialDelaySeconds: 10
            timeoutSeconds: 3
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 65532
          volumeMounts:
            - name: socket
              mountPath: /run/csi
          {{- with .Values.sidecars.livenessProbe.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: socket
          emptyDir: {}
        {{- if .Values.mobility.enabled }}
        - name: webhook-tls
          secret:
            secretName: {{ include "shiftpv.fullname" . }}-webhook-tls
        {{- end }}
