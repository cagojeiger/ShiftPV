apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shiftpv-filesystem-fault
spec:
  storageClassName: shiftpv-filesystem-fault
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 64Mi
